import React from 'react';
import ReactDOM from 'react-dom';

class BookDetails extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div><p>wtf</p></div>
    );
  }
}

ReactDOM.render(<BookDetails />, document.getElementById('main-panel'));
